<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.apap.director"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk
            android:minSdkVersion="22">
    </uses-sdk>
    <application
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher">
        <activity
                android:name=".LoginActivity"
                android:label="@string/app_name">
                <!--android:theme="@android:style/Theme">-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AuthUserActivity"></activity>
        <activity android:name=".ContactsActivity"></activity>
        <activity android:name=".InboxActivity"></activity>
        <activity android:name=".NewMsgActivity"></activity>
    </application>
</manifest>
